

To build a docker image run the following command from `catalogic-api-server` project root directory

1. 
```shell
./gradlew clean build 
```

2. 
```shell
docker build . -t catalogic-api-server
```

